# Интернет-магазин на FastAPI

Это **учебный проект** интернет-магазина, созданный с использованием **FastAPI** и **SQLAlchemy**.  
Проект позволяет **управлять товарами, категориями, отзывами и пользователями**.

---

## 🚀 Возможности

- **Получение списка товаров**
- **Создание, обновление, удаление товаров**
- **Создание отзывов о товарах**
- **Регистрация и аутентификация пользователей**
- **Административные функции**
- **Рейтинг товаров** на основе отзывов

---

## 🧩 Используемые технологии

- **Python 3.10+**
- **FastAPI** — веб-фреймворк
- **SQLAlchemy** — ORM для работы с базой данных
- **PostgreSQL** — основная база данных
- **Alembic** — миграции
- **Pydantic** — валидация данных
- **JWT** — аутентификация

---

## 🧑‍💻 Установка

1. Клонируй репозиторий:

   ```bash
   git clone https://github.com/splashblack1/fastapi_ecommerce.git
   cd fastapi_ecommerce
   
2. Создай виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```
3. Установи зависимости:
```bash
pip install -r requirements.txt
```
4. Создай .env файл (на основе .env.example):
```bash
cp .env.example .env
# Заполни своими данными
```
5. Запусти миграции:
```bash
alembic upgrade head
```
6. Запусти сервер:
```bash
uvicorn app.main:app --reload
```
